name: rastreio_pnz
description: Versão brasileira do USPSTF - Recomendações preventivas de saúde
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  flutter_widget_from_html: ^0.15.0
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  flutter_html: ^3.0.0-beta.2 

flutter:
  uses-material-design: true
  
  assets:
    - assets/data/
